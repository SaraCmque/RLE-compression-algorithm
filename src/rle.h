#ifndef RLE_H
#define RLE_H

#include <string>

// Declaración de la función para comprimir usando RLE
std::string comprimirRLE(const std::string& texto);

// Declaración de la función para descomprimir usando RLE
std::string descomprimirRLE(const std::string& textoComprimido);

#endif // RLE_H